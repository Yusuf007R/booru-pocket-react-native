//Base api url

const baseUrl = 'https://danbooru.donmai.us';
const SafebaseUrl = 'https://safebooru.donmai.us';
//generic error

const genericError = 'Ups! try again';

export {genericError, baseUrl, SafebaseUrl};
